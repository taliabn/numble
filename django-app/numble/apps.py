from django.apps import AppConfig


class NumbleConfig(AppConfig):
    """
    Configuration settings for numble app
    """

    default_auto_field = "django.db.models.BigAutoField"
    name = "numble"
